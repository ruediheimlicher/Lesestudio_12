{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {cancelClick = id; }; 
            CLASS = rProgressDialog; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                cancelButton = id; 
                exportProgressPanel = id; 
                progressBar = id; 
                statusField = id; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}